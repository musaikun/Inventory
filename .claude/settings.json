{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"${CLAUDE_TOOL_INPUT:-}\" | grep -q '\"app/src' && echo '--- build check ---' && cd /home/user/Inventory/app && npm run build 2>&1 | grep -E '(error|✓ built|x Build|Build failed)' | head -5 || true"
          }
        ]
      }
    ]
  }
}
