# WEB-001 — Web Free版をproduction公開可能にする

- 状態の正本は [`../task-list.md`](../task-list.md)
- 公開判定の正本は [`../web-release-readiness.md`](../web-release-readiness.md)
- 初回read-only監査: 2026-08-04 / Codex
- 品質基盤更新: 2026-08-08 / Codex
- production変更: 未着手（WEB-01のUser判断待ち）

## Scope

棚卸効率化を中心とするFree版をWeb/PWAとして公開する。公開時に約束する主経路は、
account登録、品目準備、棚卸の開始・中断・再開・完了、複数端末同期、別端末での履歴詳細、
CSV書出し、account削除とする。

入出庫と発注確認はClaude Codeが既存実装を保持したままβ機能へ分離する。主要導線と公開文面で
正式な在庫管理・発注送信として約束せず、中核dataの安全性を損なわないことをrelease条件とする。
14日trial、Stripe、Pro販売、PostHog有効化、Google Play/TWAは含めない。

## 初回preflight（2026-08-04 / read-only）

- 基準: `develop@bc9fb85`。作業開始時のworktreeはclean。
- develop Actions run `30882005257` はWorker/App test、App build、develop Pages preview deployに成功。
  production deployの証拠ではない。
- 稼働を確認できたPagesは`develop.inventory-app-c40.pages.dev`。文書に残る
  `inventory-app.pages.dev`は522で、実projectのproduction `inventory-app-c40.pages.dev`は旧build。
  canonicalを未決のまま推測しない。
- develop previewでは`/privacy(.html)`、`/terms(.html)`、`/support(.html)`が308自己redirect loop。
  `_redirects`のextensionless rewriteとPages Clean URLsのcanonicalizationを実環境で整合させる必要がある。
- remote Workerはhealth 200だが、任意Originを`Access-Control-Allow-Origin`へ反射する旧挙動を確認。
  repositoryのfail-closed実装はproductionへ未deployと判定する。
- repository側の既定`ALLOWED_ORIGIN`/Pages suffixは実際のPages project hostと一致しないため、
  canonical確定→設定/test更新→Worker deploy→実probeの順が必要。
- 本番D1は既存記録上0010/0011未適用。現行movement/account deletionは両migrationを前提とする。
- `scripts/deploy.sh`のPages deployはproduction branchを明示せず、developからの実行結果が曖昧。
  Wrangler版、resource、commit SHA、rollbackを含むproduction手順の固定が必要。

## Workstreams

1. Userがcanonical URLと正式contactを決める。
2. Claude Codeが品目取込、`DATA-001`/`DATA-002`、過去棚卸取込、β機能境界を実装し、
   task単位のtest結果と残riskを引き渡す。
3. Codexが各差分を独立reviewし、公開scope、data safety、認可、回帰testとの整合を判定する。
4. CodexがCORS、Pages production deploy、migration順、rollback、`SEC-005`、Free 2台上限、
   `OPS-001`、`TEST-002`の公開条件を整える。
5. User承認後だけmigration/deployし、production smokeとrollback確認を記録する。

## 完了条件

- [`../web-release-readiness.md`](../web-release-readiness.md)の`WEB-01`〜`WEB-10`が完了する。
- 棚卸中心のrelease contractを満たし、β機能が正式機能と誤認されない。
- Web P0が0件で、残るWeb P1にrelease影響、owner、期限、回避策がある。
- 対象commit付きのtest/build/audit、production URL、smoke結果を記録する。
- CodexとClaude Codeが[`../quality-scorecard.md`](../quality-scorecard.md)を独立採点し、低い方でも基準を満たす。
- Userがrelease candidateと公開継続を承認する。

## 未実施

production deploy、D1 migration、外部service変更、commit、push。
