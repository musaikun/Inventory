# DOC-001 — 現行文書をWeb先行の正本体系へ整理

- 状態の正本は [`../task-list.md`](../task-list.md)
- 着手: 2026-08-04 / Codex

## 監査範囲

- 初回90 Markdown、並行追加された`UI-001.md`を含む最終91 Markdownと`docs/export/`のZIPを棚卸し。
- 正本、現行候補、runbook/draft、将来設計、履歴へ分類。
- Web/Play/課金方針、公開gate、task状態、検証値、相対linkを横断確認。

## 初回検出

- 同名の`project-status.md`が2冊あり、双方が現況に見える。
- 品質基盤、scorecard、task優先度がGoogle Play先行のまま。
- D-016のPlay先行と最新の公開順が衝突し、Android trial/Web Stripeの将来フローが判断記録に未反映。
- README/task boardに`develop@7d47cb4`、未commit差分、古いtest件数が残る。
- `spec.md`、`api-design.md`、`sync-spec.md`、`security-review.md`、
  `test-cases.md`等に現行実装との差がある。
- `bug-reports.md`にrepository root基準で書かれた相対linkが41件あり、
  quality-foundation directoryから解決しない。

## Phase 1（2026-08-04 完了）

- [x] `docs/README.md`を唯一の総合索引として新設する。
- [x] Web release gate、WEB-001、D-021を新設する。
- [x] task boardとagent入口をWeb先行へ同期する。
- [x] Play資料、dated audit、export、完了記録は削除・移動せず役割を明示する。
- [x] 壊れた相対linkを修正し、path-level検査を通す。

## Phase 1検証

- docs配下の全ローカルMarkdown link target: 解決。
- `git diff --check`: 成功（Windows改行形式warningのみ）。
- 既存のdated audit/export本文、App/Worker code: 変更なし。
- code test/build: 文書のみの変更のため未実行。成功とは報告しない。

## Phase 2（2026-08-06 完了）

- [x] `spec/api/sync/security`を現行code・migrationへ照合し、W1 baselineとknown gapを追加。
- [x] `test-cases`を恒久回帰仕様、`test-checklist-new-features`を履歴snapshotへ分離。
- [x] `ci-cd`を現行workflowへ照合し、preview実績とproduction gapを分離。
- [x] `docs/README.md`、鮮度台帳、project overviewを完了状態へ同期。
- [x] 大量rename・directory移動はlink churnを避け、Web公開後の独立判断へ送る。

## 最終検証

- 全92 files（Markdown 91 + export ZIP 1）を分類。物理移動・削除なし。
- docs配下の全ローカルMarkdown link target: 解決。
- Markdown table列: 崩れなし。
- `git diff --check`: 成功（Windows改行形式warningのみ）。
- DOC-001担当によるApp/Worker code変更: なし。並行追加されたUI-001のApp差分は保持し、対象外。
- DOC-001としてcode test/buildは未実行。UI-001の独立検証と混同しない。
- production deploy、migration、外部service変更、commit、push: 未実施。

## 完了条件

- [x] 読む人が現在仕様、将来計画、履歴を混同しない。
- [x] Web公開のblockerと後続Play/Stripe項目が別の正本から追跡できる。
- [x] release-critical文書がcodeと採用済み判断に一致し、known gapをtaskへ追跡できる。
- [x] local Markdown linkが解決し、検証日・対象commitのない成功実績を現在値として扱わない。
