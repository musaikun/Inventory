# UI-004 — Riveの試験環境

## 範囲

- 着手日: 2026-09-08。Userの「環境を整えます」を受けたローカル開発環境の準備。
- Rive Webランタイム、Vueでの読み込み・破棄、ローカル`.riv`の試験ページと手順を用意する。
- 演出の内容・採否・振り分け画面への組み込み方はUserが判断する。UI-003の製品画面は変更しない。
- Web release gate、API、DB、認可、業務データ、app versionは変更しない。

## 完了条件

- 依存関係をlockfileへ固定し、WASMを同じpackageから読み込む。
- `.riv`をローカルで読み込み、再生・停止・再読込を試せる。
- 不正ファイルのエラー表示、サイズ変更、画面非表示時の停止、破棄に対応する。
- 導入手順、対象commit、実行した検証と未実施の実機確認を記録する。

## 実装

### 2026-09-11 追補: User依頼のcommit・push準備

- Userの「一旦ここまでをpush」を受け、Rive環境・素材・UI-005の公開準備・関連文書を`ec4adcaf5ec2e45b09831c6c4dba89c1e8519bab`でcommit。メッセージは`feat(rive): add wheel animation lab and runtime preparation`。
- commit前`npm.cmd run build`成功（493 modules）。ステージ後の`git diff --cached --check`でSVGの末尾空白を検出したため、生成スクリプトで除去して再生成。修正後は指摘なし。
- `origin/develop@95f2b25239cc18a864efcf79f89efeee88b9a6d4`の5件を取り込み。コード競合なし。`proposals.md` / `session-log.md`の追記競合は双方を保持。リモートでUserが決めた0.93.0を保持し、lockfileの自アプリ情報2箇所も0.93.0へ整合（依存の追加更新なし）。
- 統合後の対象は上記2commitのmerge作業ツリー。別件HistoryCalendarのローカル差分も存在する状態で検証しており、clean commit単体の検証ではない。
- `npm.cmd test -- --reporter=dot --pool=threads --maxWorkers=2`: 151 files / 1622 tests passed、11 skipped。`App.deleteBack` / `App.reloadPage`でhook timeout、別4ファイルでworker起動timeoutによりexit 1。全件成功とはしない。
- 該当6ファイルだけ再確認: `npm.cmd test -- src/App.deleteBack.test.js src/App.reloadPage.test.js src/components/AxisAssignFocus.groups.test.js src/App.complete.test.js src/components/InventoryTable.hiddenList.test.js src/composables/useConfig.replenish.test.js --reporter=dot --pool=threads --maxWorkers=1 --hookTimeout=30000` → **6 files / 128 tests passed、exit 0**。テスト実装は変更なし。
- 統合後`npm.cmd run build`: 成功（498 modules、PWA 17 entries / 2724.99 KiB）。`npm.cmd run build:rive`: 成功（25 modules）。既存のCJS/chunk size警告のみ。
- 別件`HistoryCalendar.vue` / `HistoryCalendar.plan.test.js`は今回のcommitから除き、作業中にステージされた内容も保持する。push先は通常の`origin/develop`。手動deployやmigration適用は行わない。pushによる既存のpreview workflowは起動する。
- Rive Editor上の制作物はプラン制限で書き出せず、Gitに入るのはSVG・確認PNG・設定の検証記録・試験環境。`.riv` / `.rev`本体、製品UIへの組み込み、スマホ性能の確認は未完了。

### 2026-09-10 追補: Rive Editorでの制作・検証

- Userの「インストールしました」を受け、起動中Desktop Editorの`http://127.0.0.1:9791/mcp`へ接続成功（server v0.6）。Codex設定・契約の変更なし。
- 空の[Untitled / file 2564146](https://editor.rive.app/file/untitled/2564146)へ`AssignWheel`アートボード（375×336）を作成。SVGを編集可能な図形として取り込み、Textで仮の英字名称・数量を配置。
- 既定Timelineを`Closed`へ、既定State Machineを`Wheel`へ改名し再利用。`Open`を追加。View Model `AssignWheel` / Instance `Default` / Boolean `isOpen`を作成・アートボードへbindし、false→Closed / true→Openとした。両遷移700ms、cubic-bezier(0.65, 0, 0.35, 1)。
- MCP `simulateStateMachine`（160 frames / 60fps）: 無入力でEntry→Closed（遷移1回）、frame5=true / frame50=falseでEntry→Closed→Open→Closed（遷移3回、frame111でsettled）。記録は`app/dev/rive/verification/wheel-editor.json`。
- 早い反転（frame30=false）は開く700msの遷移終了後に閉じる。frame70の停止後入力ではシミュレーターが再始動しなかった。実際のEditor操作・Web再生時の再開と連打は未確認で、成功としない。
- `capture_artboard`で開・閉を750×672のPNGへ出力し目視確認。試作ルートがアートボード外の位置・倍率になったため基準値x/y=0, scale=100%へ戻して再取得し、最終検証後も基準値を確認。PNG2点とEditorリンクを試験ページへ追加。画像は静止画であり、動画/性能測定ではない。
- 日本語は既定Interで字形欠け。Google Fonts公式のNoto Sans JPを承認付きでTEMPへ取得したが、MCPのTTF uploadが`not enabled for this editor`で拒否。Editor内は仮英字とし、フォント選択は後続。TTFはrepoへ追加していない。
- **`export_file(format: riv)`はworkspaceプラン制限で拒否**。`.riv` / `.rev`ともアップグレード対象との応答を受け、再試行・他形式での迂回・契約変更はしていない。制作物はEditor上にあり、アプリ用ファイルは未生成。
- 固定アートボード内での開閉のみ。ページ高336→56、可変幅、動的分類/件数、日本語、ドラッグ・慣性、業務操作は未接続。採否・次の演出・プラン変更はUser判断。
- 対象`develop@bfb4c9acac5273720f055f7c5f890f4d60cf38f9` + working tree。`npm.cmd run build:rive`成功（25 modules）、HTML/PNG2点/ガイドHTTP 200、PNG MIME/シグネチャ確認、`git diff --check`指摘なし。今回の変更はdevページ・素材・文書で、App全体testは9/8の154 files / 1723件成功から再実行していない。
- `public/_headers` / `vite.config.js`に別担当UI-005の変更が増えたことを確認し保持。READMEの公開準備をUI-005参照へ更新。HistoryCalendarの既存差分も保持。commit / push / deployなし。
- UI-004はレビュー待ち / User。書き出し後のWeb・スマホ実再生/性能は未完了。

### 2026-09-08 追補: 既存UIからの素材案

- Userから「まだ素材は出来ていない。既存のUIを参考に用意できるか」と依頼。
- `AxisAssignFocus.vue`の青・枠色・56pxカード・角丸12・件数バッジ・64px操作領域・700msの扇補間を参照し、`dev/rive/WheelDraft.vue`と`wheelDraft.mjs`へSVGでの開閉試作を追加した。
- 対象の動きを非同期で尋ね、回答未着のため、最初のたたき台はホイールの展開・収納と明示して作成。採用判断はUserに残す。
- `dev/rive/public/assets/wheel-open.svg` / `wheel-closed.svg`は375×336 / 375×56、共通28個の名前付きレイヤー。文字・件数を焼き込まず、図形を編集できる形で出力。試験ページだけに仮ラベルを表示。
- 既存CSSの3D投影を2Dの位置・スケールへ近似している。影・上下フェードは未収録。**SVG試作はRiveの実描画ではなく、性能比較にも使わない**。
- 起動済み試験ページ上部で展開・収納、400/700/1000ms、開き具合のスライダーを操作できる。SVGと制作手順をダウンロード可能。再生成は`node dev/rive/export-wheel-draft.mjs`。
- 公式資料でRive Desktop MCP（`http://127.0.0.1:9791/mcp`）の図形・キーフレーム・ステートマシン編集を確認。この環境ではinitialize疎通が`ECONNREFUSED`、利用可能browserも0件。**Rive Editorへの変更・`.riv`生成・書き出しは未実施**。
- 公式手順には通常DesktopとEarly Accessの記載が混在しているため、MCP対応版のEditorが必要。runtime exportは有料プランの対象。接続・課金変更・インストールは行っていない。
- 検証（同じ`bfb4c9a` + working tree）: `npm.cmd run build:rive`成功（23 modules）、2SVGのXML・ID重複なし・script/image/textなしを確認。101段階の中間SVGで非有限座標なし。HTML・コンポーネント・素材・制作手順をHTTP 200、SVGを`image/svg+xml`で確認。`git diff --check`指摘なし。
- 追補はdev試作と文書だけのため、環境構築時のApp全体testや通常App buildは再実行していない。実ブラウザ目視・スマホ・Riveインポートは未確認。製品画面と既存のHistoryCalendar差分は保持。
- 手順: [`素材制作ガイド`](../../../app/dev/rive/public/assets/wheel-rive-guide.md)。参照: [公式SVG](https://rive.app/docs/editor/assets/svg)、[公式MCP](https://rive.app/docs/editor/ai/mcp)、[公式runtime export](https://rive.app/docs/editor/exporting/exporting-for-runtime)。

### 環境構築

- `@rive-app/webgl2@2.42.0`をexact指定・lockfileへ追加。既存依存の版変更はない。
- `app/src/utils/riveRuntime.js`: 主WASMと旧端末用fallbackを同じnpm packageからURL import。外部CDNへfallbackしない。
- `app/src/components/RiveCanvas.vue`: 素材指定時の遅延読込、contain描画、DPR上限2、非表示・reduced motion時の描画停止、resize、cleanup、古い読込通知の無効化、エラー表示。
- `app/dev/rive/`: ブラウザ内でのローカルファイル読込、アートボード・ステートマシン選択、再生・一時停止・読み直し、表示幅切替。画像・フォント・音声は素材へ埋め込む前提。
- `npm run dev:rive`で`http://127.0.0.1:5174/`を起動。`npm run build:rive`は`app/dist-rive/`へ出力（Git管理対象外）。通常のApp buildは試験ページを含まない。
- 手順と手動確認台本は[`app/dev/rive/README.md`](../../../app/dev/rive/README.md)。設計判断は`docs/proposals.md`へ投稿。

## 検証

対象: `develop@bfb4c9acac5273720f055f7c5f890f4d60cf38f9` + 本タスクのworking tree。
開始前からの`HistoryCalendar.vue` / `HistoryCalendar.plan.test.js`差分も保持した状態で検証。

- `npm.cmd run build:rive`: 成功。20 modules、主WASM 2,148,863 bytes・fallback 2,161,296 bytesを別ファイル出力。
- `npm.cmd run build`: 成功。493 modules、PWA 17 entries / 2705.92 KiB。既存CJS・500 kB超chunk警告あり。`dist/`にRive/WASM出力はない。
- `npm.cmd run dev:rive`: localhost:5174で起動成功。Node fetchでHTML・JSと両WASMのHTTP 200を確認。両WASMは`application/wasm`、`WebAssembly.validate()`成功。
- 初回のWASM疎通スクリプトはPowerShell→Node標準入力で日本語パスが`?`に化けて失敗。パスを`process.cwd()`から取得するスクリプトで上記を確認した。製品コードの障害ではない。
- `npm.cmd test -- --reporter=dot --maxWorkers=2`: 150 files / 1600 tests passedだが、forks worker起動timeoutが4件ありexit 1。この実行は全件成功とはしない。
- 再確認`npm.cmd test -- --reporter=dot --pool=threads --maxWorkers=2`: **154 files / 1723 tests passed、exit 0**。追加したRiveの6件も含む。
- `git diff --check`: whitespaceエラーなし（LF→CRLF警告のみ）。
- `npm.cmd audit --omit=dev --json --fetch-retries=0 --fetch-timeout=10000`: 既存依存にhigh 2件（`nanoid`, `pdfjs-dist`）、moderate 1件（`dompurify`）。Riveへの指摘なし。scope外のため自動修正・major更新は行わない。
- `npm.cmd outdated --json --fetch-retries=0 --fetch-timeout=10000`: 既存9依存の更新候補を確認。Riveは導入時latest。更新は未実施。
- インストール時に既存`@zxing/library@0.22.0`のNode >=24要件とローカルNode 22.14.0の差を警告。今回追加したRiveはこの警告の対象ではない。
- 試験buildとdev起動は、初回sandboxの親directory読取制限で失敗。承認された制限外の同一commandで成功。

## 共通DoDと未実施

- UI: 試験用ページのみ。空・読込中・エラー表示、44px以上の操作部、reduced motion対応を実装。375px / tablet / PC実表示・WebGL性能は未確認。
- 入力・データ: ローカル`.riv`をメモリ内で扱う。業務データ、localStorage、account切替、D1、config、同期、認可、プラン、通知は変更なしのためN/A。
- 通信・エラー: WASMを同一originから配信。読込エラーは表示し再選択できる。業務画面への適用とPWA offline対応は後続。
- ナビゲーション: 独立した開発用ページ。AppのBack制御・スワイプ・WSへ影響なしのためN/A。
- テスト: ランタイムをmockし、破棄・旧callback無効化・非表示復帰・reduced motion・エラー回復・resizeの6件を追加。WASMの実描画を検証するテストではない。
- Browserスキルの接続を試し、troubleshootingと一覧確認後も利用可能browserは0件。実際の`.riv`描画・スマホ操作・フレームレートは未確認。
- 公開へ組み込む前に、現在のCSPのWASM許可と`.riv`/`.wasm`のPWA cacheを判断する。今回は公開headerを変更しない。
- commit / push / deploy / migrationは未実施。app versionは0.92.0のまま。
