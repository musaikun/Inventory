# SEC-005 — 無制限な店舗作成経路を整理

- 状態の正本は [`../task-list.md`](../task-list.md)

- 根拠: `/auth/register` と `worker/src/index.js:118-121` の legacy `/store/create` に、
  濫用防止と明確な廃止条件がない。
- 完了条件:
  - legacy 経路を廃止または明示的に保護する。
  - 登録の rate limit と bot 対策方針を決める。
  - 正常登録、過剰試行、legacy 呼び出しをテストする。

## 着手順（2026-08-08 確定）

`DATA-002` の Phase 1（`GET /store/:code/sessions/:id/lines` の追加）が
`worker/src/index.js` の同じ store ルート群に入るため、**着手順を固定する**。

> **`DATA-002` Phase 1 の完了後に本タスクへ着手する。**
>
> **2026-08-08: Phase 1 完了。着手可。** `index.js` の store ルート群に
> `GET /store/:code/sessions/:id/lines` が入っている（`_requireAuth` の内側、
> `/sessions/:id/complete` の直前）。この追加後の状態を基点にすること。

- 担当: 本タスク = Codex ／ `DATA-002` = Claude Code
- Phase 1 を先にする理由は、本番で実害が出ており、User承認済みの復旧対象データ
  （2026-07-07 の351行）が `inventory_lines` に残ったままのため
- CC は Phase 1 完了時に Codex へ着手可を通知する。通知前に本タスクを開始すると同一ファイルで競合する
- 同じ順序を [`DATA-002.md`](DATA-002.md) にも記載している
