# バックログ

## 📍 現在地（2026-06-27 / v0.3.0）

直近セッションで完了済み（=このリストの下部の旧項目は多くが解決済み）:
- D1永続化・認証・複数端末同期・バーコード・PDF遅延読込 … 実装済み
- ゲスト追加→ホスト承認フロー
- 無料プラン制限（150品目／履歴直近1回／3台、CSV取込は無料、TWAは価格非表示）
- 招待リンク共有（ネイティブ共有/LINE/メール）・TOPにバージョン表示
- ホスト同時ログインを1端末に制限（単一ホストセッション）
- ヘッダーのハンバーガーメニュー化・「空のリスト/練習モード」開始・品目追加フォームのトグル
- テスト: app 171 / worker 65、戦略=docs/strategy-10yr.md

### ▶ 次にやる（フェーズ1の締め）
1. **ネットワークエラーUX** — オフライン表示・D1保存失敗の再送インジケータ（記録の正の信頼性）
2. **品目マージUI** — 棚卸後に類似品目を統合（データ品質＝フェーズ3の燃料）

### 推奨ワークフロー（次セッション）
- タスク単位でセッションを切る（戦略議論と実装を混ぜない）
- 一次レビューは `/code-review`、定型の確定は `/ship`（ビルド→コミット→プッシュ）
- AI/MCP は フェーズ1完了後（データが貯まってから）

---

## 🔴 高優先度（※下記は旧記載。D1・バーコード等は解決済み）

### 店舗コード方式の外部データ永続化
- localStorage 依存の解消（端末変更・担当者交代に対応）
- Cloudflare D1 に品目リスト・棚卸履歴を保存
- 認証済み端末なら任意端末から同じデータにアクセス可能

### PDF処理の軽量化（iPhone SE2 で処理が完了しない）
- 現状: pdfjs-dist をクライアント側で全ページ処理
- 対策案（優先順）:
  1. Cloudflare Worker でサーバー側テキスト抽出（スマホCPU負荷ゼロ）
  2. PDF廃止・Excel統一（運用対応）
- 遅延読み込み・進捗表示・40秒タイムアウトは実装済み

## 🟡 中優先度

### Google OAuth 認証
- 機種変更・パスワード忘れを Google 側で解決
- 店舗コード → Google アカウントに紐付け
- 現時点では「端末固定運用が多い」ため後回し

### PIN忘れ時の復旧（バックアップエクスポート）
- shopCode + hostToken を JSON でダウンロード
- 機種変時にインポートで完全復旧

## 🟢 低優先度（中長期）

### バーコードスキャン（設計済み・未実装）
- **方針**: `config.codes` フィールドをバーコードとして流用（スキーマ変更不要）
- **ライブラリ候補**: `html5-qrcode`（JAN/QR/Code128 など全形式対応）
- **UXフロー**:
  1. メイン画面にカメラボタン追加（音声ボタン隣）
  2. スキャン → 登録済みコードなら即品目特定 → 数量入力へ
  3. 未登録なら「品目名を入力」ミニ画面 → 登録して以降は自動認識
- **品目一覧**: 各品目にバーコード登録ボタン（SettingsModal内）
- **Excel取込拡張**: バーコード列を自動検出（13桁数字 or ヘッダー名マッチ）
- **実装しない理由（現時点）**: 使用予定なし・ライブラリ追加でバンドル増加
- **実装するタイミング**: 業種拡張フェーズ（小売・倉庫・医療など）

### Phase 2 分析機能
→ `docs/phase2.md` 参照

### localStorage 暗号化
- 外部DB移行（高優先度項目）で自然解決予定
