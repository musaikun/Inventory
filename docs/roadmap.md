# ロードマップ（実行計画）

最終更新: 2026-07-15 / 対応バージョン v0.48 系
位置づけ: 「何を・どの順で・なぜ」を1枚にした実行計画。戦略（`strategy-10yr.md`）と
現在地（`project-status.md`）の下に立つ、near-term の“地図”。
`👉` は Claude の意見（採否は要判断）。状態: ✅完了 / 🔧一部 / 📄設計のみ / ⬜未着手

> 2026-07-15: v0.35.2 時点の記述を v0.48 実態に同期（入出庫✅・発注アシスト✅・E0土台🔧）。
> あわせて全体レビュー（`holistic-review-2026-07.md`）の必須項目を F/G 節と Wave 2.5 に反映。

---

## 0. アプリの目標（オーナーの言葉）

- 棚卸の高速化 ／ ルームで複数人同時 ／ 品目インポート・エクスポート
- 紙とペンの脱却 ／ 棚卸の属人化の脱却
- **導入3000店舗を見据える**（＝“今作る”ではなく“詰まらない設計＋指標で発火”）

## 原則（意思決定の軸）

> **蛇口（現場高速化）→ 燃料（データ捕捉）→ 価値（分析・コスト）→ 配送車（企業インフラ）。**
> **買うのは現場ではなく上層部。** 有料価値は買い手の言語（削減額・ロス・人時・発注）で語る。
> PMF（上層部が金を払う実証）より先にスケールへ過剰投資しない。

---

## 1. トラック別 課題インベントリ

### A. 現場の定着（リテンション＝蛇口を枯らさない）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| ネットワークエラーUX（オフライン表示・保存失敗の再送） | ✅ | 接続バナー＋D1保存失敗の自動再送（v0.35.4）＋再接続時のオフライン分岐マージ/競合ルーティング（ホスト v0.35.5・ゲスト v0.35.6） |
| 品目マージ | ✅ | 完全一致は入口で拒否済（addItem/CSV統合）で当面十分。**表記ゆれの自動検出は誤検出・取りこぼしが多く不採用**（例: サーモン/鮭/スモークサーモン は文字列で拾えない）。将来は「手動マージ＝エイリアス学習の入口」として小さく足す（任意） |
| ゲーム性・達成感 | ⬜ | 使い続ける動機 / 中。👉**「継続日数」は棚卸が低頻度（週/月）なので不向き。**自己ベスト更新（前回より速い）・完了バッジ・担当者別スピードなど“1回ごとの達成”に寄せるべき |
| 天気の表示（事前予報＋過去実績の確定） | 🔧 | `HistoryCalendar` に天気表示スロットは実装済み。**実データ連携（Open-Meteo等・鍵不要）が未**。真価は「各棚卸に天気タグ＝需要/発注分析の燃料」 |

### B. 上層部価値（分析・レポート・管理者ページ）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| 在庫分析ダッシュボード（在庫金額・前回差・ABC・信頼度） | ✅ | 単店の基礎分析（ManagerDashboard） |
| **管理者ページ** | 🔧 | 上層部が見る面。👉Bの各分析＋下記レポートの器。単店（ダッシュボード）はある。多店舗集計は未 |
| **経営レポート出力（PDF/メール月次）** | ⬜ | **売るための成果物**。上層部はアプリにログインしない / 中。👉既存データで作れる最短の“武器” |
| **行動ログ→生産性分析** | ⬜ | 人時生産性・担当者別スピード・棚卸所要時間の推移。👉`auditLog`/`participants`/`activeMs` は**取得済み**＝新データ不要。独自性が高い |
| 多店舗集計基盤（本部×店舗） | 📄 | 上層部・企業価値の実エンジン → `enterprise-design.md` / 大 |
| 履歴カレンダー（棚卸/発注/入出庫の統合表示） | ✅ | v0.48。日付ベース・種別フィルタ・金額概算・詳細シート |

### C. マネタイズ基盤
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| E0: サーバー側プラン管理（`isPro()` をサーバー判定へ） | 🔧 | **土台は導入済み（C-01 / 2026-07-15）**: `stores.plan` 列＋14日トライアル導出（`entitlements.js`）、`/auth/*` が `{ plan, isPro, inTrial }` を返す。**残**: 無料の床のゲート確定＋`LIMITS_DISABLED`/`tanaoro_is_pro` の撤去（クライアント配線）→ `pricing-strategy.md` |
| B2B料金体系（組織契約・席数/店舗数・請求書払い） | ⬜ | 現行は消費者型。3000店舗の契約形態が未設計 / 中。👉インボイス（適格請求書）発行可否もここで決める |
| 決済（Stripe） | 📄保留 | サンドボックス検証待ち。👉導入時はメール収集が実質必須 → プライバシーポリシー改定・特商法表記とセット（→G法務） |
| **ドメイン取得** | ⬜ | B2B信用の最低条件（今は `*.pages.dev`）/ 小・即 |

### D. データ捕捉（燃料の拡張）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| 棚卸スナップショット（在庫断面） | ✅ | 既存の主データ |
| 行動ログ・実働時間（誰が/何を/いつ/何個/何分） | ✅ | 取得＆履歴永続化済み。分析面が無いだけ（→B） |
| **仕入（入出庫）の捕捉** | ✅ | v0.48 `useMovements`＋`MovementPage`。発注→入庫のワンタップ取込・理論在庫（`theoreticalStock`）まで実装。**残: D1同期（現状 localStorage 専用・下記F参照）** |
| 天気タグ付き履歴 | 🔧 | 表示スロットのみ（→A天気と同源） |

### E. 発注アシスト（数値獲得の楔）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| 発注アシスト＆分析基盤 | ✅ | v0.48 実装済み（発注セッション・適正在庫の曜日別学習・推奨発注数・理論在庫プリフィルとズレ検出）。設計 → `ordering-analytics-design.md`（F=ミッションは未実装） |
| 発注点（手動床＋データ推奨の二段構え） | 🔧 | 手動発注点 `reorderPoints`・要補充判定・暫定「目安」実装済み（2026-07-17・提案採用 → `proposals.md`）。算出発注点は需要モデル完成後 |
| 需要カレンダー・発注スケジュール | 🔧 | 暦の需要要因（祝日・連休・給料日・長期休暇）の層と発注曜日/締切（Phase A）を実装（2026-07-18取込・レビューは順次） |
| 入数の量子化（ケース入数の推定） | ⬜未解決 | 👉**ブロッカーではない**。受入数量の履歴から推定（最頻値/GCD）→確認→**信頼度ゲート**で解放。完璧を待たず信頼度バッジ付きで出す |
| 消費量の逆算＋需要ベース発注予測 | ⬜ | データの環（発注→入庫→棚卸）は完成済み。**実データが貯まってから**精度検証 |

### F. インフラ / スケール / 運用（3000店舗の非機能）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| **入出庫の D1 同期** | ⬜**必須** | 現状 localStorage 専用＝端末故障で消える。「無料の床＝入出庫」戦略の前提条件（消えると★1）。S-10 の残課題でもある / 中 |
| **日次バックアップ（cron → R2 エクスポート）** | ⬜**必須** | D1 Time Travel(30日)のみは「記録の正」の看板に不十分。リストア手順の検証まで含めて1項目（C-04）/ 中 |
| 監視・アラート（Sentry or Logpush＋失敗率） | ⬜ | 「どの店舗で何が失敗しているか」が見えないと有料サポート不能。課金開始前に最小構成 / 小〜中 |
| 認証テーブル掃除 cron（auth_tokens/login_attempts/ip_attempts） | ⬜ | 肥大化防止（S-H）/ 小 |
| inventory_lines の R2 アーカイブ | ⬜ | D1 10GB上限＝最初に来る天井。指標で発火 / 中 |
| ライブ在庫の書き込み削減（`saveInventoryToD1`） | ⬜ | 最大の無駄書き込み源。安価な即効策 / 小 |
| store_history に session_id 列（全件パース排除） | ⬜ | 無認証 result エンドポイントの DoS 耐性にも直結（監査 スケール#3）/ 小 |
| 組織ごとD1分割（サイロ型・10倍成長時） | 📄 | → `db-design-v2.md` / 大 |
| 監視・障害復旧 runbook・ステータスページ | ⬜ | 運用の空白。契約前に整える / 中 |
| 前提: Workers Paid プラン | — | 無料は書込10万/日で即オーバー |

### G. セキュリティ / 法務 / 配布
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| **セキュリティ小改修3点**（CORSフェイルクローズ＋`ALLOWED_ORIGIN`本番設定 / CSP `_headers` / `/pdf` 認証・サイズ上限・レート制限） | ⬜**必須** | 監査 S-E/S-07/S-D。いずれも小改修。課金開始後は「有料サービスの脆弱性」に格上げされる / 小 |
| **WSゲスト向け prices サニタイズ** | ⬜**必須** | 原価がアルバイト端末に見える（S-G/C-02）。企業商談の失注要因 / 小 |
| **退会・アカウント削除フロー** | ⬜**必須** | `stores.deleted_at` はあるが API/UI 無し（C-03）。**Google Play はアカウント削除手段（アプリ内＋Web URL）を義務化**＝Play公開のブロッカー / 中 |
| **法務: 特商法表記・ポリシー改定（メール収集）・総額表示** | ⬜**必須(課金時)** | 有料化と同時に法的必須。Stripe→メール収集でプライバシーポリシー改定 / 小 |
| PIN 強化第2弾（6桁化 or 頻出PIN拒否・shopCode×IP併用カウント） | ⬜ | S-04 残課題（スプレー攻撃）/ 小 |
| アカウント復旧手段（リカバリーコード or メール） | ⬜ | PIN忘れ＝完全ロックアウト（C-07）。課金開始前に / 中 |
| トークン保存時ハッシュ化 | ⬜ | D1流出時の被害縮小（S-H）/ 小 |
| 店舗間データ分離の監査 | 🔧 | D1 は全クエリ `WHERE shop_code=?` 分離済み（S-10調査で検証）。定期的な再監査をルール化 / 小 |
| 企業向け認証（PIN以上・SSO等） | ⬜ | PINは企業には弱い / 中 |
| **連携API（外部サービス）** | 🔧 | 内部API設計書は既存（`api-design.md`）。👉**公開API契約は課金する連携先が出るまで作らない** |
| **Google Play 無料版（機能制限＋TWAラッピング）** | 🔧 | `isTwaApp()`・価格非表示・リーダー文言は実装済。残: ①無料版の機能境界（→pricing-strategy）②TWAのビルド/署名/公開パイプライン ③Data Safety フォーム（操作ログ・IPログの申告整合）④アカウント削除要件（上記）/ 中 |

### H. コード健全性（技術的負債）
| 項目 | 状態 | 目的 / 依存 / 規模 |
|---|---|---|
| R-01: `App.vue`（3,400行）の解体 | ⬜ | 同期バグ混入率に直結（B-01型事故の温床）。次の大機能の前に専用セッションで / 大 |
| R-02: `useSync.js` のエミッタ化（setter 25個） | ⬜ | 同上。R-01とセット / 中 |
| E2Eスモークテスト1本（ログイン→ホスト→ゲスト→同期→再接続） | ⬜ | 核心リスク（同期）がユニットで守れない。Playwright / 中 |

---

## 2. 実行ウェーブ（クリアする順）

### Wave 0 — 蛇口を守る ✅ 完了（v0.35系）
1. ネットワークエラーUX（A）✅ / 2. 品目マージ（A）✅
3. ドメイン取得（C・即／オーナー作業）⬜**残**

### Wave 1 — 最速の“上層部wow”（新データ不要）⬜
5. 行動ログ→生産性分析（B）＋ 経営レポート出力（B）＝**管理者ページの第一形**
   👉既存の `auditLog`/`activeMs`/スナップショットだけで作れる。差別化の一番槍

### Wave 2 — 燃料を増やす→発注へ ✅ ほぼ完了（v0.48）
6. 仕入（入出庫）の捕捉（D）✅
7. 天気タグ付き履歴＋現場フック（A/D）🔧 表示スロットのみ・実データ連携残
8. 発注アシスト（E・信頼度ゲート／入数推定）✅（入数推定・パー・ミッションは残）

### Wave 2.5 — 信頼性と収益化準備（2026-07 全体レビューの必須項目）★次はここ
9. セキュリティ小改修3点＋WS prices サニタイズ（G・半日規模）
10. 入出庫の D1 同期（F・料金戦略の前提）
11. 日次バックアップ cron→R2＋リストア検証（F）
12. 退会・アカウント削除フロー（G・Play要件）
13. 最小監視（F）／認証テーブル掃除cron（F）

### Wave 3 — 配布・拡張・企業インフラ（指標で発火）
14. Google Play 無料版の機能境界＋TWA公開（G）※12が前提
15. 多店舗集計基盤・管理者ページの本部版（B）
16. **E0 完遂＋料金プラン実装**（C・`pricing-strategy.md` のリバーストライアル）→ 法務セット（特商法・ポリシー改定）→ B2B料金・請求書払い・企業認証（C/G）
17. スケール天井: 書き込み削減→session_id列→R2アーカイブ→D1分割（F）
18. 連携API（外部）（G・引き合い後）

---

## 3. 判断メモ

- **課金基盤(E0)**: 土台（サーバー側プラン列＋トライアル導出）は 2026-07-15 導入済み。無料/PRO境界の確定と
  クライアント配線（`LIMITS_DISABLED` 撤去）は料金プラン実装（Wave 3）と同時に行う。
- **Wave 2.5 を新設した理由**: 収益化・Play公開・企業商談のいずれに進んでも前提になる項目
  （データ耐久性・バックアップ・削除フロー・既知の小穴）だけを先に潰す。機能追加はこの間凍結しない
  （並行可）が、**Wave 3 に入る前に完了させる**。
- **管理者ページ** は独立機能ではなく「B（分析）＋レポートの器」。まず単店の生産性/レポートで立ち上げ、多店舗集計は Wave3。
- **API** は“準備”＝内部境界を綺麗に保つこと。公開契約は需要が出てから（早すぎる公開は負債）。
- **ゲーム性** は継続日数より“1回ごとの達成”。**天気** は現場UXより分析データとして効く。
- 3000店舗は「設計で詰まらせない＋指標で発火」。Wave3は前倒ししない。
