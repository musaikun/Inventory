# CLAUDE.md — 飲食店棚卸管理システム

## プロジェクト概要

飲食店の棚卸作業を高速化し、食材の実績使用量と理論使用量の乖離・原価率の異常を可視化するツール。

**コンセプト**
- 入力は最小化（音声ベース）
- 分析は自動化
- 現場で使える速度と精度

---

## 開発フェーズ

### MVP（現在対応中）
- [x] 音声入力 → 食材データ化（スマホWebアプリ）
- [ ] 棚卸表通りの並び順表示
- [ ] 辞書による表記ゆれ統一
- [ ] 数量入力・確認UI
- [ ] CSVコピー出力

### Phase 2（後回し）
- [ ] CSV読込・分析（PC側 Python/pandas）
- [ ] 理論使用量 vs 実績使用量の差分計算
- [ ] 原価率算出
- [ ] PDF/グラフレポート出力
- [ ] 発注点アラート
- [ ] ITEM_META による単位換算（袋→g など）

---

## システム構成

```
スマホ（入力）           PC（分析・後回し）
├─ 音声入力              ├─ CSV取込
├─ 辞書引き              ├─ 分析処理（pandas）
├─ 棚卸表UI              └─ PDF出力
└─ CSV出力
```

---

## 技術スタック

| レイヤー | 技術 | 理由 |
|---|---|---|
| フロントエンド | HTML + CSS + Vanilla JS | サーバー不要、スマホブラウザで即動作 |
| 音声認識 | Web Speech API（ja-JP） | iOS/Android Chrome/Safari対応、無料 |
| データ保存 | localStorage（当日分） | オフライン対応、インストール不要 |
| 辞書・並び順 | config.js（JS定数） | ノンエンジニアでも編集しやすい |
| 出力 | CSV（クリップボードコピー） | そのまま貼り付け可能 |
| 将来の分析 | Python + pandas | PC側で処理 |

**Flutterは使用しない。Webアプリとして開発する。**

---

## ファイル構成

```
inventory/
├── CLAUDE.md               # このファイル
├── README.md
├── app/
│   ├── index.html          # スマホ用 棚卸入力UI
│   ├── style.css           # モバイルファーストCSS
│   ├── app.js              # 音声認識・パース・辞書引きロジック
│   └── config.js           # 辞書（DICTIONARY）+ 並び順（INVENTORY_ORDER）
└── data/（将来）
    ├── sales.csv           # 売上データ
    ├── purchase.csv        # 仕入データ
    └── recipe.csv          # レシピデータ
```

---

## データフォーマット

### 棚卸出力CSV
```
date,ingredient,quantity,unit
2026-04-07,"コーヒー豆　ブラジルNo.2　中煎り　1kg袋",3,袋
2026-04-07,"牛乳　成分無調整　1Lパック",5,パック
```

### config.js の構造
```javascript
// 表記ゆれ辞書: { エイリアス: 正式名称 }
const DICTIONARY = {
  "ブラジル": "コーヒー豆　ブラジルNo.2　中煎り　1kg袋",
  "プレモル": "サントリー　プレミアムモルツ（青）　生樽10L　香るエール",
  // ...
};

// 棚卸表の並び順（この順番でUI表示・CSV出力）
const INVENTORY_ORDER = [
  "コーヒー豆　ブラジルNo.2　中煎り　1kg袋",
  // ...
];
```

---

## 音声入力の仕様

### 入力フォーマット
```
「食材名 + 数量（+ 単位）」
例: ブラジル 3袋 / 牛乳 5 / エスプレッソ 2
```

### 処理フロー
```
音声 → テキスト化
  → 数量・単位・食材名を分解
  → DICTIONARY で正式名称に変換（曖昧一致）
  → 候補が複数 → 選択モーダル表示
  → 確認モーダル（数量編集可）
  → 棚卸一覧に反映
```

---

## 設計上の決定事項

### ① 同じ商品の再入力
- 確認ダイアログで「上書き」or「追加」を選択させる
- 例: "すでに 3袋 入力済みです。[上書き: 2袋] [追加: 3+2=5袋]"

### ② 小数点・単位換算
- **MVP**: 0.5袋はそのまま `0.5袋` として保存（小数OK）
- **Phase 2**: `ITEM_META` に換算係数を追加して g/ml 変換対応
  ```javascript
  // Phase 2 で追加予定
  const ITEM_META = {
    "コーヒー豆　ブラジルNo.2　中煎り　1kg袋": {
      unit: "袋", unit_amount: 1000, unit_base: "g"
    }
  };
  ```

### ③ 辞書マッチング
- 完全一致 → 即確認モーダル
- 部分一致（複数候補） → 選択モーダル（最大5件）
- 不一致 → 入力テキストをそのまま品目名として確認モーダルへ

### ④ データ永続化
- localStorage に当日分のみ保存
- 翌日は自動リセット（日付が変わったら破棄）

---

## 開発ブランチ

```
開発ブランチ: claude/restaurant-inventory-system-0XNHA
ベース: main
```

---

## 実装済み機能（現状）

- [x] 音声入力 → 食材データ化
- [x] テキスト検索・辞書マッチング・候補選択モーダル
- [x] 数量入力UI（テンキー・プリセット・音声・単位）
- [x] CSV / PDF / Excel 品目リスト取込
- [x] CSVエクスポート
- [x] カテゴリ別表示・並べ替え（五十音/学習順/ジャンル）
- [x] 棚卸履歴（スナップショット保存・月次比較）
- [x] マルチデバイスWebSocket同期（Cloudflare DO）
- [x] ルームコード・QRコード参加
- [x] チャット機能（LINE風・返信・メンション・履歴保持）
- [x] 完了通知・競合通知（別端末が同じ品目を更新した際）
- [x] 品目リストのリアルタイム同期（ホスト更新→ゲスト反映）
- [x] ConfirmModal のリアルタイム更新（モーダル開放中も在庫変更を反映）
- [x] セキュリティ基礎（Origin検証・参加者上限・レート制限・ルームコード6文字）

---

## バックログ（未実施・優先度順）

### 🔴 高優先度

#### 品目変更履歴（ルーム内アクティビティログ）
- ルーム内で誰がいつ何を登録・変更・上書きしたかを表示
- DO に `auditLog[]` を保持（最大200件）
- SyncModal または専用パネルで時系列表示
- 品目名でフィルター可能
- 詳細設計は下記「品目変更履歴 仕様メモ」参照

#### 店舗コード方式の外部データ永続化
- localStorage 依存の解消（端末変更・担当者交代に対応）
- Cloudflare D1（SQLite）に品目リスト・棚卸履歴を保存
- 永続的な「店舗コード」で任意端末からアクセス
- アカウント不要・コード共有モデル

### 🟡 中優先度

#### ホスト認証
- 現状: 誰でも `role:host` を名乗れる
- ルーム作成時にサーバー側でトークン発行 → 再接続時に検証
- 設計変更が必要（中規模）

#### オフライン→再接続時のデータマージ
- 現状: 再接続時にサーバー在庫がローカルを上書きする
- タイムスタンプベースのマージロジックが必要
- オフライン中の変更を失わないようにする

#### DOMPurify 導入
- チャットの `v-html` に対する多層防御
- 現状の自前エスケープは正しいが将来の改修リスクへの保険
- `npm install dompurify` + ChatModal.vue の renderText に適用

### 🟢 低優先度（中長期）

#### PDF処理の軽量化（iPhone SE2 で処理が完了しない問題への対策）
- 現状: pdfjs-dist（1.2MB）をアプリ起動時に読み込み、クライアント側で全ページ処理
- 対策候補（優先度順）:
  1. 遅延読み込み（dynamic import）— PDF選択時のみpdfjs読み込み。起動軽量化のみ
  2. Cloudflare Worker でサーバー側テキスト抽出 — スマホのCPU負荷をほぼゼロに
  3. PDF廃止・Excel統一（運用対応）— 処理コスト最小
- 進捗表示・キャンセル・40秒タイムアウトは実装済み（cf2cd4f）

#### localStorage 暗号化
- 店舗コード方式の外部DB移行で自然解決予定
- 先行実装は費用対効果低

#### Phase 2 分析機能
- CSV読込・分析（Python/pandas）
- 理論使用量 vs 実績使用量の差分計算
- 原価率算出・PDF/グラフレポート出力
- 発注点アラート
- ITEM_META による単位換算（袋→g など）

---

## 品目変更履歴 仕様メモ

### データ構造（Durable Object）
```javascript
auditLog: [
  {
    ingredient: 'ビール（生樽）',
    action: 'update',       // 'update' | 'remove'
    qty:    5,
    unit:   '本',
    prevQty:   3,           // 変更前の数量（初回登録は null）
    prevUnit:  '本',
    enteredBy: 'Aさん',
    timestamp: 1714000000000,
  },
  ...  // 最大200件、古いものから削除
]
```

### 表示場所の候補
- **案A（推奨）**: SyncModal に「変更履歴」タブを追加
  - 全品目の変更を時系列で表示（最新50件）
  - 品目名で絞り込みフィルター
- **案B**: ConfirmModal の入力済み警告の下に品目ごとの直近3件表示

### 同期フロー
```
ホスト/ゲストが update/remove
  → Worker が auditLog に追記
  → 全員に { type: 'audit_entry', ...entry } をブロードキャスト
  → クライアントがローカルの auditLog 配列に追記
```

### 表示フォーマット
```
14:23  Aさん  ビール（生樽）  3本 → 5本
14:21  Bさん  日本酒（獺祭）  新規 2本
14:18  Aさん  ビール（生樽）  新規 3本
```

---

## 将来の分析機能（仕様メモ）

```
実績使用量 = 期首在庫 + 仕入 - 期末在庫
理論使用量 = 商品販売数 × レシピ使用量
差分（ロス）= 実績使用量 - 理論使用量
ロス金額   = 差分 × 単価
原価率     = 理論原価 / 売上
```

分析軸: 食材別 / 週別 / 商品別（将来）

---

## 非機能要件

- 棚卸入力: リアルタイム（音声認識後即反映）
- オフライン動作: 必須（現場のWifi環境が不安定なケースに備える）
- 操作: 入力ステップ最小化、ワンタップ修正可能
- 精度: 完全一致不要、傾向把握が目的
